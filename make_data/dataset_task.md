### 任务目标：基于 MoVQGAN 的果汁异常检测数据集构建与微调准备

本任务旨在构建一套用于 LLaMA-Factory 微调的 ShareGPT 格式数据集。通过 MoVQGAN 提取正常果汁样本的离散 Token，并结合增强后的异常样本图像，构建“图像输入-离散 Token 回归-异常诊断”的复合训练任务。

---

### 阶段一：正常样本离散化（Token Extraction）
**流程：** 参考 `MoVQGAN/sft_data.py` 的编码逻辑，对所有类别的正常果汁图片进行特征提取。
1.  **分目录处理**：按果汁类别（如 Orange, Apple 等）遍历图片，确保类别隔离。
2.  **唯一标识化**：图片名采用 `类别_UUID` 或 `哈希值` 命名（例如：`orange_ca9a282c9e774`）。
3.  **Token 序列化**：
    * 将每张图片转换为长度为 384 的离散 Token 序列。
    * **输出格式**：保存为 JSON 映射表，格式为 `{"图片唯一ID": [Token1, Token2, ... Token384]}`。
    * **用途**：该序列将作为后续微调任务中“理想正常状态”的回归目标。

---

### 阶段二：异常样本图像增强（Data Augmentation）
**流程：** 为提升模型泛化能力，需对各类别异常果汁图像进行数据增强。
1.  **增强策略**：采用常规图像处理手段（包括但不限于：随机旋转、亮度调整、对比度增强、轻微裁剪等）。
2.  **规模控制**：设置可调参数 `aug_count`，规定每类异常样本增强后的目标总数。
3.  **严格一致性**：增强过程必须严格保持类别标签的一致性，严禁出现跨类别的数据污染（例如：Orange 的异常图严禁配对 Banana 的正常 Token）。

---

### 阶段三：ShareGPT 格式微调数据集制作（Instruction Synthesis）
**流程：** 将异常图像与对应类别的正常 Token 组合，生成符合 LLaMA-Factory 规范的训练数据。
1.  **存储路径**：所有图像统一存放至 `LLaMA-Factory/data/` 目录下。
2.  **数据配对逻辑**：
    * **Input**：一张增强后的异常图片。
    * **Label (Target)**：从同类别正常 Token 库中随机抽取一组 Token 序列。
3.  **数据格式规范**：
    ```json
    {
        "id": "orange_uuid_001",
        "images": ["data/anomaly_orange_v1.png"],
        "system": "你是异常检测专家，负责产品质量鉴定。",
        "conversations": [
            {
                "from": "human",
                "value": "你眼前的是一瓶果汁，但该产品存在异常。请结合你对正常产品的认知进行对比，并还原该产品理想状态下的样子。最后请判断该果汁是否有异常，并指出异常位置。"
            },
            {
                "from": "gpt",
                "value": "<|8641|><|14042|><|1602|>...[384个Token]...<|14888|> \n以上是该类果汁正常状态的特征映射。对比可见，当前产品存在异常。\n<|endoftext|><|im_end|>"
            }
        ]
    }
    ```

---

### 核心开发约束
* **类别强校验**：建立 `Category-Lock` 机制，确保异常图片与回归 Token 属于同一果汁品类。
* **Token 封装**：回归部分的 Token 需严格使用 `<|ID|>` 格式进行特殊编码封装。
* **自动化流水线**：脚本需支持一键完成“离散化 -> 增强 -> 序列化”全流程。